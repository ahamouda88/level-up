package com.levelup.service;

import com.levelup.persist.model.Account;

/**
 * An interface that extends {@link GenericService} and manages the logic for performing operations on an
 * {@link Account} entity
 * 
 * @author ahamouda
 *
 */
public interface AccountService extends GenericService<Account, String> {
}
